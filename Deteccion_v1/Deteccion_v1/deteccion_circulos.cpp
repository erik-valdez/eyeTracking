//
//  deteccion_circulos.cpp
//  Deteccion_v1
//
//  Created by Erik Valdez on 06/09/18.
//  Copyright © 2018 Erik Valdez. All rights reserved.
//

#include "deteccion_circulos.hpp"

/*
vector<Vec3f> circles;

/// Apply the Hough Transform to find the circles
HoughCircles( gris, circles, CV_HOUGH_GRADIENT, 1, gris.rows, 40, 25,5, 15 );//10 50

/// Draw the circles detected
for( size_t i = 0; i < circles.size(); i++ )
{
    Point center(cvRound(circles[i][0]), cvRound(circles[i][1]));
    int radius = cvRound(circles[i][2]);
    // circle center
    circle( frame, center, 3, Scalar(0,255,0), -1, 8, 0 );
    // circle outline
    circle( frame, center, radius, Scalar(0,0,255), 3, 8, 0 );
}

/// Show your results
namedWindow( "Hough Circle Transform Demo", CV_WINDOW_AUTOSIZE );
imshow( "Hough Circle Transform Demo", frame);
namedWindow( "cc", CV_WINDOW_AUTOSIZE );
imshow( "cc", gris);

if(cv::waitKey(1) >= 0) break;


}//end while
*/

//-----------------------------------------


//detector.detectMultiScale(procesada, rect);// deteccion co la funcion xml
  //if(cv::waitKey(1) >= 0) break;
